# FOROZ Website

Monorepo for the FOROZ nonprofit's main website.

- `frontend/` — React + Vite + Tailwind + shadcn/ui
- `backend/` — Express API (contact, newsletter, donations, applications)

Each subproject has its own `package.json` and README with setup instructions.
